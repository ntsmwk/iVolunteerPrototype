/**
 * Write your model definitions here
 */

namespace at.jku.cis

participant Marketplace identified by id {
  o String name
  o String id
}

asset SimpleHash identified by hash {
  o String hash
}

transaction postSimpleHash {
  o String hash
}

transaction removeSimpleHash {
  o String hash
}