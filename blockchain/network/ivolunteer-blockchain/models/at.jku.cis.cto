/**
 * Write your model definitions here
 */

namespace at.jku.cis

participant Marketplace identified by id {
  o String name
  o String id
}

asset SimpleHash identified by hash {
  o String hash
}

asset GlobalHash identified by hash {
  o String userId
  o String hash
}

transaction postSimpleHash {
  o String hash
}

transaction postGlobalHash {
  o String userId
  o String hash
}